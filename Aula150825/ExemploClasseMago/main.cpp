#include "Mago.h"

int main()
{
    // Instanciando um objeto da classe mago
    Mago mago;

    // Invocando os métodos
    mago.andar();
    mago.falar("Olá Mundo!");
    mago.invocarMagia();

    return 0;
}